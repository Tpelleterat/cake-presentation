trigger:
  - main
  
pool:
  vmImage: 'ubuntu-18.04'

steps:
  - task: UseDotNet@2
    inputs:
      version: '6.0.x'

  - task: DotNetCoreCLI@2
    displayName: 'Restore dotnet tools'
    inputs:
     command: 'custom'
     custom: 'tool'
     arguments: 'restore'

  - bash: |
     dotnet cake